apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: flask-app
data:
  DB_HOST: postgres-service
  DB_PORT: "5432"
  DB_NAME: appdb
  REDIS_HOST: redis-service
  REDIS_PORT: "6379"
  ENVIRONMENT: production
  APP_VERSION: "1.0.0"